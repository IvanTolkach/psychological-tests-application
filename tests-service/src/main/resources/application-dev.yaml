spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/psychologicaltestsdb?currentSchema=tests
    username: postgres
    password: 1111
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      port: 6379
      host: localhost

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
    jersey:
      enabled: false